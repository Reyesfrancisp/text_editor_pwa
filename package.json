{
  "name": "JATE",
  "version": "1.0.0",
  "description": "Your PWA description here",
  "main": "server.js",
  "scripts": {
    "build": "cross-env NODE_ENV = webpack && production",
    "build:prod": "webpack --mode=production",
    "start": "node server.js",
    "heroku-postbuild": "cd client && npm install && npm run build",
    "install": "npm install",
    "server:dev": "cd ./server && npm run server",
    "client:dev": "cd ./client && npm start",
    "dev": "concurrently \"npm run server:dev\" \"npm run client:dev\""
  },
  "keywords": [],
  "author": "Your Name",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "if-env": "^1.0.4"
  },
  "devDependencies": {
    "concurrently": "^5.2.0",
    "cross-env": "^7.0.3",
    "nodemon": "^2.0.4"
  }
}
